---
phase: 07-advanced-examples-legends
plan: 04
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - mermaid-tw5/tiddlers/Architecture Diagram.tid
  - mermaid-tw5/tiddlers/Mindmap.tid
  - mermaid-tw5/tiddlers/Timeline.tid
  - mermaid-tw5/tiddlers/Sankey Diagram.tid
  - mermaid-tw5/tiddlers/XY Chart.tid
  - mermaid-tw5/tiddlers/Block Diagram.tid
  - mermaid-tw5/tiddlers/Packet Diagram.tid
  - mermaid-tw5/tiddlers/Kanban Board.tid
  - mermaid-tw5/tiddlers/Quadrant Chart.tid
autonomous: true
requirements: [EXAMPLE-01, LEGEND-02]
must_haves:
  truths:
    - "All 9 existing Format A tiddlers carry the MermaidExample tag and a %%-commented advanced real-world example (EXAMPLE-01)"
    - "Block Diagram and Timeline include a color-by-category legend; the other 7 do not (LEGEND-02 / D-05)"
    - "Existing basic example, syntax, and tips sections are preserved (expand, not rewrite)"
  artifacts:
    - path: "mermaid-tw5/tiddlers/Block Diagram.tid"
      provides: "Expanded block-beta example with style-category legend"
      contains: "tags: MermaidExample"
    - path: "mermaid-tw5/tiddlers/Timeline.tid"
      provides: "Expanded timeline example with section-color legend"
      contains: "tags: MermaidExample"
    - path: "mermaid-tw5/tiddlers/Sankey Diagram.tid"
      provides: "Expanded sankey-beta example, no ampersand in node names"
      contains: "tags: MermaidExample"
  key_links:
    - from: "mermaid-tw5/tiddlers/Architecture Diagram.tid"
      to: "Mermaid Chart Catalog"
      via: "[[← Mermaid Chart Catalog]] backlink (already present)"
      pattern: "Mermaid Chart Catalog"
---

<objective>
Expand the 9 existing Format A example tiddlers: add the `tags: MermaidExample` field,
add or upgrade a `%%`-commented advanced real-world example, and add a WikiText legend
table to the two that color by category (Block Diagram, Timeline). Preserve each file's
existing intro, basic example, syntax, and tips sections (D-01 "expand, not rewrite").

Purpose: EXAMPLE-01 requires every in-scope type to have an advanced, well-commented
example; these 9 already exist as Format A but predate the EXAMPLE-03 tag and lack the
`%% ── section ──` advanced treatment (PATTERNS Pattern 5). Sankey must keep its Phase-6
`&`-free node names (Pitfall 5).
Output: 9 modified `.tid` files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-advanced-examples-legends/07-CONTEXT.md
@.planning/phases/07-advanced-examples-legends/07-RESEARCH.md
@.planning/phases/07-advanced-examples-legends/07-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand Architecture Diagram, Mindmap, Timeline</name>
  <files>mermaid-tw5/tiddlers/Architecture Diagram.tid, mermaid-tw5/tiddlers/Mindmap.tid, mermaid-tw5/tiddlers/Timeline.tid</files>
  <read_first>
    - mermaid-tw5/tiddlers/Architecture Diagram.tid — the file being modified AND the reference template; it already has a real-world section (add tag + `%%` banners only)
    - mermaid-tw5/tiddlers/Mindmap.tid and mermaid-tw5/tiddlers/Timeline.tid — current contents to preserve and expand
    - 07-PATTERNS.md §"Pattern 5: Format A Expand Operation" (what to keep vs add) and §"Pattern 2" (`%% ── section ──` banner style)
    - 07-RESEARCH.md §"Legend Scope Matrix (D-05)": Architecture NO, Mindmap NO, Timeline YES (section period colors); §"Pitfall 6" mindmap (spaces only, no tabs) and timeline (no `:` in event labels)
  </read_first>
  <action>
    For each file: add `tags: MermaidExample` as the line directly after the `type:`
    front-matter line (do not disturb `created`/`title`/`type`; bump `modified` to
    `20260609000000000`). Ensure a `!! Real-world example: <subtitle>` section exists with
    a fenced source block followed by a matching `$$$text/vnd.tiddlywiki.mermaid` block,
    the advanced source using `%% ── section ──` banner comments. Architecture Diagram:
    its real-world cloud example already exists — add the tag and add `%% ── section ──`
    banners to the advanced block (keep icons among `cloud`/`server`/`database`/`disk`/
    `internet`). Mindmap: keyword `mindmap`, indentation with SPACES only (no tabs);
    NO legend. Timeline: keyword `timeline`, multi-`section` real-world example, no `:`
    inside event labels; ADD a legend using the simpler `|!Section|!Color|!Meaning|`
    variant mapping cycled section colors to periods (LEGEND-02). Keep every existing
    basic/syntax/tips section and the `[[← Mermaid Chart Catalog]]` footer.
  </action>
  <verify>
    <automated>for f in "Architecture Diagram" "Mindmap" "Timeline"; do grep -q 'tags: MermaidExample' "mermaid-tw5/tiddlers/$f.tid" && grep -q 'Real-world example' "mermaid-tw5/tiddlers/$f.tid" && grep -q 'Mermaid Chart Catalog' "mermaid-tw5/tiddlers/$f.tid" || exit 1; done; grep -qE '\|!.*\|!' "mermaid-tw5/tiddlers/Timeline.tid" && node --test tests/phase07-structure.test.js 2>&1 | grep -qE '# fail 0'</automated>
  </verify>
  <acceptance_criteria>
    - All three files contain `tags: MermaidExample`, a `!! Real-world example` heading, and the `[[← Mermaid Chart Catalog]]` footer
    - `Timeline.tid` contains a WikiText legend table; `Architecture Diagram.tid` and `Mindmap.tid` do NOT
    - `Mindmap.tid` indentation uses spaces (no literal tab characters in the `$$$` blocks)
    - No file contains `<$mermaid>` or a theme `%%{init}%%`
    - Existing basic example / syntax / tips sections still present in each file
    - `node --test tests/phase07-structure.test.js` reports `# fail 0`
  </acceptance_criteria>
  <done>Architecture, Mindmap, Timeline expanded with tag + advanced section; Timeline has a legend.</done>
</task>

<task type="auto">
  <name>Task 2: Expand Sankey Diagram, XY Chart, Block Diagram</name>
  <files>mermaid-tw5/tiddlers/Sankey Diagram.tid, mermaid-tw5/tiddlers/XY Chart.tid, mermaid-tw5/tiddlers/Block Diagram.tid</files>
  <read_first>
    - mermaid-tw5/tiddlers/Sankey Diagram.tid, XY Chart.tid, Block Diagram.tid — current contents (Sankey already has the Phase-6 `&`→"and" fix; preserve that)
    - 07-PATTERNS.md §"Pattern 5" and §"Pattern 3" (legend table for Block)
    - 07-RESEARCH.md §"Pitfall 5" (no `&`/`'`/`/`/`-` in Sankey node names) and §"Legend Scope Matrix (D-05)": Sankey NO, XY Chart NO, Block Diagram YES (`style` for component categories)
    - 07-RESEARCH.md §"Per-Type Structural %%{init}%%" — XY Chart `{"xyChart":{"width":800,"height":400}}`; §"Bundle facts to verify" — do NOT use Sankey v11.15.0+ keys (`labelStyle`,`nodeWidth`,`nodePadding`,`nodeColors`) since bundle is 11.14.0
  </read_first>
  <action>
    For each file: add `tags: MermaidExample` after the `type:` line, bump `modified`.
    Ensure a `%%`-commented `!! Real-world example` section exists. Sankey Diagram: keyword
    `sankey-beta`, real-world energy/budget flow; ALL node names must avoid `&`/`'`/`/`/`-`
    (use "R and D" style); do NOT add Sankey config keys unverified in 11.14.0; NO legend.
    XY Chart: keyword `xychart-beta`, real-world revenue/metric chart with both bar and
    line, optional `%%{init}: {"xyChart": {"width": 800, "height": 400}}`; NO legend.
    Block Diagram: keyword `block-beta`, real-world layered-architecture example using
    `style` to color component categories (close every `block` with `end`); ADD a
    `|!Color|!Meaning|` legend whose swatches match the `style ... fill:#...` values
    (LEGEND-02). Keep all existing sections and the catalog footer.
  </action>
  <verify>
    <automated>for f in "Sankey Diagram" "XY Chart" "Block Diagram"; do grep -q 'tags: MermaidExample' "mermaid-tw5/tiddlers/$f.tid" && grep -q 'Real-world example' "mermaid-tw5/tiddlers/$f.tid" || exit 1; done; grep -q '|!Color|!Meaning|' "mermaid-tw5/tiddlers/Block Diagram.tid" && ! grep -q '&' "mermaid-tw5/tiddlers/Sankey Diagram.tid" && node --test tests/phase07-structure.test.js 2>&1 | grep -qE '# fail 0'</automated>
  </verify>
  <acceptance_criteria>
    - All three files contain `tags: MermaidExample` and a `!! Real-world example` heading
    - `Block Diagram.tid` contains a `|!Color|!Meaning|` table matching its `style fill:#...` values; `Sankey Diagram.tid` and `XY Chart.tid` do NOT
    - `Sankey Diagram.tid` contains NO `&` character anywhere (Pitfall 5 + structure test (e))
    - `Sankey Diagram.tid` uses `sankey-beta`; `XY Chart.tid` uses `xychart-beta`; `Block Diagram.tid` uses `block-beta` with matching `end` for each `block`
    - No file contains `<$mermaid>` or a theme `%%{init}%%`
    - `node --test tests/phase07-structure.test.js` reports `# fail 0`
  </acceptance_criteria>
  <done>Sankey, XY Chart, Block Diagram expanded with tag + advanced section; Block has a legend; Sankey stays ampersand-free.</done>
</task>

<task type="auto">
  <name>Task 3: Expand Packet Diagram, Kanban Board, Quadrant Chart</name>
  <files>mermaid-tw5/tiddlers/Packet Diagram.tid, mermaid-tw5/tiddlers/Kanban Board.tid, mermaid-tw5/tiddlers/Quadrant Chart.tid</files>
  <read_first>
    - mermaid-tw5/tiddlers/Packet Diagram.tid, Kanban Board.tid, Quadrant Chart.tid — current contents to preserve and expand
    - 07-PATTERNS.md §"Pattern 5" (expand operation) and §"Pattern 2" (banner comments)
    - 07-RESEARCH.md §"Legend Scope Matrix (D-05)": Packet NO, Kanban NO, Quadrant NO (all three skip — field/column/quadrant labels are self-documenting); §"Tiddler Inventory" canonical keywords `packet-beta`, `kanban`, `quadrantChart`
  </read_first>
  <action>
    For each file: add `tags: MermaidExample` after the `type:` line, bump `modified`.
    Ensure a `%%`-commented `!! Real-world example` section exists with the
    fenced+`$$$` pair. Packet Diagram: keyword `packet-beta`, real-world protocol frame
    (e.g. TCP or a custom binary header) with bit ranges; NO legend. Kanban Board: keyword
    `kanban`, real-world sprint board with multiple columns and cards; NO legend. Quadrant
    Chart: keyword `quadrantChart`, real-world 2x2 prioritization (e.g. effort vs impact)
    with axis labels, quadrant labels, and plotted points; NO legend. Keep every existing
    basic/syntax/tips section and the `[[← Mermaid Chart Catalog]]` footer.
  </action>
  <verify>
    <automated>for f in "Packet Diagram" "Kanban Board" "Quadrant Chart"; do grep -q 'tags: MermaidExample' "mermaid-tw5/tiddlers/$f.tid" && grep -q 'Real-world example' "mermaid-tw5/tiddlers/$f.tid" && grep -q 'Mermaid Chart Catalog' "mermaid-tw5/tiddlers/$f.tid" && ! grep -q '|!Color|!Meaning|' "mermaid-tw5/tiddlers/$f.tid" || exit 1; done; node --test tests/phase07-structure.test.js 2>&1 | grep -qE '# fail 0'</automated>
  </verify>
  <acceptance_criteria>
    - All three files contain `tags: MermaidExample`, a `!! Real-world example` heading, and the catalog footer
    - None of the three contains a `|!Color|!Meaning|` legend table
    - `Packet Diagram.tid` uses `packet-beta`; `Kanban Board.tid` uses `kanban`; `Quadrant Chart.tid` uses `quadrantChart`
    - No file contains `<$mermaid>` or a theme `%%{init}%%`
    - Existing basic/syntax/tips sections still present in each file
    - `node --test tests/phase07-structure.test.js` reports `# fail 0`
  </acceptance_criteria>
  <done>Packet, Kanban, Quadrant expanded with tag + advanced section; none needs a legend.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → tiddler file | Author-controlled static `.tid` content |
| tiddler content → Mermaid renderer | Client-side render under `securityLevel: 'loose'` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | `%%{init}%%` in any expanded example | mitigate | Only structural keys permitted (D-02); enforced by `tests/phase07-structure.test.js` |
| T-07-04 | Denial of Service | Sankey parse error from `&`/special chars (issue #7528) | mitigate | Task 2 forbids `&`/`'`/`/`/`-` in Sankey node names; verify greps for `&` and structure test (e) asserts no `&` |
| T-07-05 | Tampering | accidental rewrite dropping existing basic/syntax/tips content | mitigate | Acceptance criteria require existing sections to remain present; expand-not-rewrite per PATTERNS Pattern 5 |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs — content authoring only; RESEARCH Package Legitimacy Audit "not applicable" |

No high-severity threats apply: static content authoring with no new code paths, network, auth, or runtime input.
</threat_model>

<verification>
- `node --test tests/phase07-structure.test.js` reports `# fail 0`
- Full `node --test` shows no NEW failures vs baseline (27 pass / 1 pre-existing fail; plugin code unchanged)
- All 9 files carry `tags: MermaidExample` and a `!! Real-world example` section
- Manual: each of the 9 renders in the demo wiki without a parse error (phase-gate browser check)
</verification>

<success_criteria>
- 9 existing Format A tiddlers expanded with the shared tag and an advanced `%%`-commented example (EXAMPLE-01)
- Block Diagram and Timeline carry category legends (LEGEND-02); the other 7 do not (D-05)
- Sankey remains ampersand-free; no existing section content lost
</success_criteria>

<output>
Create `.planning/phases/07-advanced-examples-legends/07-04-SUMMARY.md` when done
</output>
