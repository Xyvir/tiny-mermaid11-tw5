---
phase: 07-advanced-examples-legends
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mermaid-tw5/tiddlers/Mermaid Legend Recipe.tid
  - tests/phase07-structure.test.js
autonomous: true
requirements: [LEGEND-01]
must_haves:
  truths:
    - "A reusable legend recipe tiddler documents the WikiText-table-below-diagram pattern (LEGEND-01)"
    - "The legend recipe renders a live example diagram with a matching legend table"
    - "A structural assertion test exists so every later authoring task can self-verify file/tag/lint conditions"
  artifacts:
    - path: "mermaid-tw5/tiddlers/Mermaid Legend Recipe.tid"
      provides: "Canonical LEGEND-01 recipe tiddler"
      contains: "tags: MermaidExample"
    - path: "tests/phase07-structure.test.js"
      provides: "Structural assertions for Phase 7 content (file presence, tag presence, no widget-body, no theme directives, no Sankey ampersand)"
      contains: "MermaidExample"
  key_links:
    - from: "mermaid-tw5/tiddlers/Mermaid Legend Recipe.tid"
      to: "Mermaid Chart Catalog"
      via: "[[← Mermaid Chart Catalog]] backlink"
      pattern: "Mermaid Chart Catalog"
---

<objective>
Create the canonical legend recipe tiddler (LEGEND-01) that every color-by-category
example points to, and the Phase 7 structural-assertion test that lets all later
authoring tasks self-verify file presence, tag presence, and the locked authoring
lint rules.

Purpose: LEGEND-01 is the reusable "key box" pattern (per D-04). It must exist first
because the 10 color-by-category examples reference it conceptually and the catalog
links to it. The test scaffold is the Nyquist Wave 0 dependency — RESEARCH.md marks
all structural checks as "❌ Wave 0 — must create first".
Output: One recipe `.tid` file and one Node test file with file-system assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-advanced-examples-legends/07-CONTEXT.md
@.planning/phases/07-advanced-examples-legends/07-RESEARCH.md
@.planning/phases/07-advanced-examples-legends/07-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Mermaid Legend Recipe tiddler (LEGEND-01)</name>
  <files>mermaid-tw5/tiddlers/Mermaid Legend Recipe.tid</files>
  <read_first>
    - mermaid-tw5/tiddlers/Architecture Diagram.tid — the D-01 `.tid` template skeleton (front-matter, `---` separators, heading rhythm, `[[← Mermaid Chart Catalog]]` footer) to replicate exactly
    - 07-RESEARCH.md §"Legend Recipe Tiddler Structure (LEGEND-01)" (the full body the file must contain) and §"Pattern 3: WikiText Table Legend (D-04)"
    - 07-PATTERNS.md §"mermaid-tw5/tiddlers/Mermaid Legend Recipe.tid (recipe-tiddler, net-new)" — front-matter and body structure
  </read_first>
  <action>
    Create `Mermaid Legend Recipe.tid` with front-matter `created: 20260609000000000`,
    `modified: 20260609000000000`, `title: Mermaid Legend Recipe`,
    `type: text/vnd.tiddlywiki`, `tags: MermaidExample`. Body per RESEARCH §"Legend
    Recipe Tiddler Structure": an intro sentence defining a legend, a `!! The recipe`
    section that shows the WikiText `|!Color|!Meaning|` table source in a fenced block,
    then a live `$$$text/vnd.tiddlywiki.mermaid` `flowchart LR` block with two `classDef`
    categories (`catA fill:#0d6efd`, `catB fill:#198754`) followed by the matching
    `@@background-color:...;color:#fff;padding:2px 8px;border-radius:3px;@@`-styled
    legend table whose `background-color` values exactly equal the `classDef` `fill`
    values. Add a `!! When to add a legend` section that links `[[Flowchart]]`,
    `[[Class Diagram]]`, `[[State Diagram]]` and notes `[[Pie Chart]]` / `[[Radar Chart]]`
    have native legends. Add a `!! Alternative: in-diagram subgraph` note describing the
    subgraph approach as flowchart-only and non-canonical (D-04 permits this secondary
    note). End with `[[← Mermaid Chart Catalog]]` as the last line. Do NOT include any
    `theme`/`look`/`fontFamily`/`themeVariables` in any `%%{init}%%` (D-02). Use
    `flowchart` (not `graph`) as the keyword.
  </action>
  <verify>
    <automated>test -f "mermaid-tw5/tiddlers/Mermaid Legend Recipe.tid" && grep -q 'tags: MermaidExample' "mermaid-tw5/tiddlers/Mermaid Legend Recipe.tid" && grep -q 'text/vnd.tiddlywiki.mermaid' "mermaid-tw5/tiddlers/Mermaid Legend Recipe.tid" && grep -q 'Mermaid Chart Catalog' "mermaid-tw5/tiddlers/Mermaid Legend Recipe.tid" && ! grep -E '%%\{init.*"(theme|look|fontFamily|themeVariables)"' "mermaid-tw5/tiddlers/Mermaid Legend Recipe.tid"</automated>
  </verify>
  <acceptance_criteria>
    - File `mermaid-tw5/tiddlers/Mermaid Legend Recipe.tid` exists
    - Front-matter contains `type: text/vnd.tiddlywiki` and `tags: MermaidExample`
    - Body contains at least one `$$$text/vnd.tiddlywiki.mermaid` block and a closing `$$$`
    - Body contains a `|!Color|!Meaning|` table with `@@background-color:#0d6efd` and `@@background-color:#198754` swatches matching `classDef ... fill:#0d6efd` / `fill:#198754` in the diagram
    - Body does NOT contain `%%{init}` with `theme`/`look`/`fontFamily`/`themeVariables`
    - Last line is `[[← Mermaid Chart Catalog]]`
    - Diagram uses keyword `flowchart` (not `graph`)
  </acceptance_criteria>
  <done>The legend recipe tiddler exists, renders a live legend example, and is tagged MermaidExample.</done>
</task>

<task type="auto">
  <name>Task 2: Create the Phase 7 structural-assertion test scaffold (Nyquist Wave 0)</name>
  <files>tests/phase07-structure.test.js</files>
  <read_first>
    - tests/typed-parser.test.js — the `node:test` + `node:assert` import/describe/it style used in this repo (there is NO jest/vitest; tests run via `node --test` from repo root, see .github/workflows/test.yml)
    - 07-RESEARCH.md §"Phase Requirements → Test Map" and §"Wave 0 Gaps" — the exact structural checks to encode
    - 07-VALIDATION.md §"Per-Task Verification Map" — the assertion intent
  </read_first>
  <action>
    Create `tests/phase07-structure.test.js` using `import { describe, it } from 'node:test'`
    and `import assert from 'node:assert'` and `node:fs`/`node:path`. Define a constant
    list of the 26 expected example tiddler titles plus `Mermaid Legend Recipe` (use the
    exact `.tid` filenames from the RESEARCH Tiddler Inventory: `Flowchart.tid`,
    `Sequence Diagram.tid`, `Class Diagram.tid`, `State Diagram.tid`,
    `Entity Relationship.tid`, `User Journey.tid`, `Gantt.tid`, `Pie Chart.tid`,
    `Requirement Diagram.tid`, `Git Graph.tid`, `C4 Diagram.tid`,
    `Architecture Diagram.tid`, `Mindmap.tid`, `Timeline.tid`, `Sankey Diagram.tid`,
    `XY Chart.tid`, `Block Diagram.tid`, `Packet Diagram.tid`, `Kanban Board.tid`,
    `Quadrant Chart.tid`, `Radar Chart.tid`, `Venn Diagram.tid`, `Ishikawa Diagram.tid`,
    `Treemap.tid`, `Tree View.tid`, `Wardley Map.tid`). Write a helper `existing()` that
    filters the list to files that currently exist (so this test passes incrementally as
    authoring proceeds and is fully green at phase end). Write these `it` blocks:
    (a) "every existing Phase 7 example tiddler is tagged MermaidExample" — for each
    existing file assert its contents match `/^tags:.*MermaidExample/m` (strip leading
    `#` comment lines is not needed; tiddler front-matter has no comments);
    (b) "no Phase 7 example uses the $mermaid widget body" — assert each existing file
    does NOT contain the literal `<$mermaid>` open tag;
    (c) "no Phase 7 example sets a theme/look/fontFamily/themeVariables in %%{init}%%" —
    assert each existing file fails the regex `/%%\{init[^}]*"(theme|look|fontFamily|themeVariables)"/`;
    (d) "every existing Phase 7 example contains a $$$ mermaid block" — assert each
    existing file contains `text/vnd.tiddlywiki.mermaid`;
    (e) "no Sankey example contains an ampersand in node content" — read
    `Sankey Diagram.tid` if it exists and assert the body contains no `&` character
    (D-08 / Pitfall 5);
    (f) "all 26 example tiddlers + legend recipe exist (phase-completion gate)" — this
    `it` asserts the full list exists; wrap its body so it only hard-asserts when an env
    flag `PHASE07_COMPLETE` is set, otherwise it logs how many of 27 exist and passes
    (so the gate is runnable mid-phase without failing). Use repo-relative path
    `mermaid-tw5/tiddlers` resolved from the test file location.
    Do NOT use watch mode. Keep total runtime well under 5 seconds.
  </action>
  <verify>
    <automated>node --test tests/phase07-structure.test.js 2>&1 | grep -qE '# fail 0'</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/phase07-structure.test.js` exists and imports from `node:test`/`node:assert` (matches repo convention; no jest/vitest)
    - `node --test tests/phase07-structure.test.js` reports `# fail 0` against the current tree (legend recipe from Task 1 present, example tiddlers not yet created — incremental-safe)
    - Test defines the full 26-title list plus `Mermaid Legend Recipe`
    - Includes the completion-gate `it` guarded by `PHASE07_COMPLETE`
    - Includes assertions for: MermaidExample tag, no `<$mermaid>` body, no theme `%%{init}%%`, presence of `$$$` block, no `&` in Sankey
    - No watch-mode flags present in the file
  </acceptance_criteria>
  <done>Structural test scaffold exists, passes against the current tree, and provides the self-verify command every later authoring task uses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → tiddler file | Author-controlled static `.tid` content; no runtime user input crosses here |
| tiddler content → Mermaid renderer | Diagram source rendered client-side under `securityLevel: 'loose'` (set in Phase 6) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | `%%{init}%%` directive in legend recipe diagram | mitigate | Lint asserts no `theme`/`look`/`fontFamily`/`themeVariables` keys (D-02); only structural keys allowed — enforced by Task 2 test (c) |
| T-07-02 | Information Disclosure | WikiText `@@...@@` inline-style legend swatch | accept | `@@css;@@` renders only inline CSS on static author text; no PII, no script execution, no data source |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — content authoring + a `node:test` file using only built-in modules. RESEARCH §"Package Legitimacy Audit" states "Section not applicable." |

No high-severity threats apply: this phase authors static TiddlyWiki content and a file-system assertion test, with no new code paths, network, auth, or runtime input handling. The only realistic concern (a content directive overriding the user's global theme) is mitigated by the D-02 lint in Task 2.
</threat_model>

<verification>
- `node --test tests/phase07-structure.test.js` reports `# fail 0`
- Full suite `node --test` shows no NEW failures vs the phase baseline (baseline: 27 pass / 1 pre-existing fail in `wrapper.test.js` "displays a friendly error message for invalid syntax", last touched commit ea71426 — unrelated to Phase 7, plugin code unchanged)
- `Mermaid Legend Recipe.tid` is tagged `MermaidExample` and contains a `$$$` block + matching legend table
</verification>

<success_criteria>
- LEGEND-01 recipe tiddler created with a live example and a color-matched WikiText legend table
- Phase 7 structural test scaffold exists, is incremental-safe, and is green against the current tree
- No theme/look/fontFamily/themeVariables directives anywhere in the recipe
</success_criteria>

<output>
Create `.planning/phases/07-advanced-examples-legends/07-01-SUMMARY.md` when done
</output>
