---
phase: 07-advanced-examples-legends
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - mermaid-tw5/tiddlers/Entity Relationship.tid
  - mermaid-tw5/tiddlers/Gantt.tid
  - mermaid-tw5/tiddlers/Pie Chart.tid
  - mermaid-tw5/tiddlers/Requirement Diagram.tid
  - mermaid-tw5/tiddlers/C4 Diagram.tid
  - mermaid-tw5/tiddlers/Defining Relationship
  - mermaid-tw5/tiddlers/Defining Relationship.meta
  - mermaid-tw5/tiddlers/Gantt 1
  - mermaid-tw5/tiddlers/Gantt 1.meta
  - mermaid-tw5/tiddlers/Pie chart diagrams
  - mermaid-tw5/tiddlers/Pie chart diagrams.meta
  - mermaid-tw5/tiddlers/Requirement Diagram
  - mermaid-tw5/tiddlers/Requirement Diagram.meta
  - mermaid-tw5/tiddlers/C4 Diagram
  - mermaid-tw5/tiddlers/C4 Diagram.meta
autonomous: true
requirements: [EXAMPLE-01, EXAMPLE-02, LEGEND-02]
must_haves:
  truths:
    - "Entity Relationship, Gantt, Pie Chart, Requirement Diagram, C4 Diagram each have an advanced Format A example (EXAMPLE-01)"
    - "The 5 stale Format B bare files + .meta sidecars are deleted, including the same-title bare files for Requirement Diagram and C4 Diagram (EXAMPLE-02)"
    - "Requirement Diagram includes a color-by-category legend (LEGEND-02); the other four do not (D-05)"
  artifacts:
    - path: "mermaid-tw5/tiddlers/Entity Relationship.tid"
      provides: "Advanced erDiagram example (no legend per D-05)"
      contains: "erDiagram"
    - path: "mermaid-tw5/tiddlers/Gantt.tid"
      provides: "Advanced gantt example (no legend per D-05)"
      contains: "gantt"
    - path: "mermaid-tw5/tiddlers/Pie Chart.tid"
      provides: "Advanced pie example (native legend, skip per D-05)"
      contains: "pie"
    - path: "mermaid-tw5/tiddlers/Requirement Diagram.tid"
      provides: "Advanced requirementDiagram example with legend"
      contains: "requirementDiagram"
    - path: "mermaid-tw5/tiddlers/C4 Diagram.tid"
      provides: "Advanced C4Context example (no legend per D-05)"
      contains: "C4Context"
  key_links:
    - from: "mermaid-tw5/tiddlers/Gantt.tid"
      to: "Mermaid Chart Catalog"
      via: "[[← Mermaid Chart Catalog]] backlink"
      pattern: "Mermaid Chart Catalog"
---

<objective>
Migrate the remaining 5 Format B canonical examples (Entity Relationship, Gantt,
Pie Chart, Requirement Diagram, C4 Diagram) into fresh Format A `.tid` tiddlers built
on the D-01 template, and delete the stale Format B originals + `.meta` sidecars. Only
Requirement Diagram colors by category and gets a legend table (LEGEND-02).

Purpose: completes the EXAMPLE-02 Format B → A migration set (11 total across plans 02
and 03). Note two of these (`Requirement Diagram`, `C4 Diagram`) have a bare Format B
file whose title equals the new `.tid` title — these are distinct filesystem entries
(no extension vs `.tid`), so the executor must delete the extension-less bare file and
its `.meta` while creating the `.tid`.
Output: 5 new `.tid` files; 10 deleted Format B files (5 bare + 5 `.meta`).

Decisions honored:
- D-06: this plan covers the remaining 5 of the ~10 catalog-referenced canonical Format B types (Defining Relationship, Gantt 1, Pie chart diagrams, Requirement Diagram, C4 Diagram); together with 07-02 this completes the 11-file migration set.
- D-07: the ~90 remaining legacy feature-snippet Format B files (Automata, Gitgraph theme variants, `interpolate*`, Subgraph 1–4, node-shape snippets, etc.) are deliberately OUT OF SCOPE — left as-is, not deleted, not upgraded, not added to the catalog. Only the canonical D-06 set is migrated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-advanced-examples-legends/07-CONTEXT.md
@.planning/phases/07-advanced-examples-legends/07-RESEARCH.md
@.planning/phases/07-advanced-examples-legends/07-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author Entity Relationship, Gantt, and Pie Chart (Format B → A migration)</name>
  <files>mermaid-tw5/tiddlers/Entity Relationship.tid, mermaid-tw5/tiddlers/Gantt.tid, mermaid-tw5/tiddlers/Pie Chart.tid</files>
  <behavior>
    - Each new `.tid` exists, tagged `MermaidExample`, contains a `$$$` mermaid block, ends with the catalog backlink
    - None of the three contains a legend table (D-05: ER, Gantt, Pie all skip — Pie has a native legend)
    - Old Format B files `Defining Relationship`, `Gantt 1`, `Pie chart diagrams` and their `.meta` sidecars are deleted
    - No file uses `<$mermaid>` body or a theme `%%{init}%%`
  </behavior>
  <read_first>
    - mermaid-tw5/tiddlers/Architecture Diagram.tid — D-01 skeleton
    - 07-PATTERNS.md §"Pattern 4: Format B Migration Structure" deletion table (rows for `Defining Relationship`, `Gantt 1`, `Pie chart diagrams`)
    - 07-RESEARCH.md §"Pitfall 6" rows for erDiagram (relationship label required `: "label"`) and gantt (`dateFormat` AND `axisFormat` both required) and §"State of the Art" (`erDiagram`, `gantt`, `pie` keywords)
    - 07-RESEARCH.md §"Per-Type Structural %%{init}%% Usage" — Gantt `{"gantt":{"barHeight":30,"displayMode":"compact"}}`; §"Legend Scope Matrix (D-05)" rows confirming ER/Gantt/Pie need NO legend
  </read_first>
  <action>
    Create three D-01 `.tid` files (front-matter `created`/`modified: 20260609000000000`,
    `type: text/vnd.tiddlywiki`, `tags: MermaidExample`; full intro→basic→real-world→
    syntax→tips→backlink structure with `%% ── section ──` banners in the advanced block).
    Entity Relationship.tid: keyword `erDiagram`, real-world schema (e.g. e-commerce)
    with cardinality crow's-foot relationships, every relationship carrying a `: "label"`;
    NO legend. Gantt.tid: keyword `gantt`, real-world project schedule with `dateFormat`
    AND `axisFormat` both set, multiple `section`s, dependencies via `after`, and optional
    `%%{init}: {"gantt": {"barHeight": 30, "displayMode": "compact"}}`; NO legend (sections
    are labeled inline). Pie Chart.tid: keyword `pie` with `title`, a real-world dataset;
    NO legend table (pie renders a native legend — note this in Tips). Then delete the old
    Format B files: `Defining Relationship`, `Defining Relationship.meta`, `Gantt 1`,
    `Gantt 1.meta`, `Pie chart diagrams`, `Pie chart diagrams.meta`.
  </action>
  <verify>
    <automated>for f in "Entity Relationship" "Gantt" "Pie Chart"; do test -f "mermaid-tw5/tiddlers/$f.tid" && grep -q 'tags: MermaidExample' "mermaid-tw5/tiddlers/$f.tid" && grep -q 'text/vnd.tiddlywiki.mermaid' "mermaid-tw5/tiddlers/$f.tid" || exit 1; done; ! grep -q '|!Color|!Meaning|' "mermaid-tw5/tiddlers/Pie Chart.tid"; test ! -e "mermaid-tw5/tiddlers/Defining Relationship" && test ! -e "mermaid-tw5/tiddlers/Gantt 1" && test ! -e "mermaid-tw5/tiddlers/Pie chart diagrams" && node --test tests/phase07-structure.test.js 2>&1 | grep -qE '# fail 0'</automated>
  </verify>
  <acceptance_criteria>
    - `Entity Relationship.tid`, `Gantt.tid`, `Pie Chart.tid` exist, each tagged `MermaidExample`, each with a `$$$` block
    - None of the three contains a `|!Color|!Meaning|` legend table
    - `Entity Relationship.tid` uses `erDiagram` and every relationship line includes a `: "..."` label; `Gantt.tid` contains both `dateFormat` and `axisFormat`
    - No file contains `<$mermaid>` or a theme `%%{init}%%`
    - Old files `Defining Relationship`(+.meta), `Gantt 1`(+.meta), `Pie chart diagrams`(+.meta) no longer exist
    - `node --test tests/phase07-structure.test.js` reports `# fail 0`
  </acceptance_criteria>
  <done>Three migrated tiddlers exist with advanced examples and no legends; the three Format B originals are gone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author Requirement Diagram and C4 Diagram (same-title Format B → A migration)</name>
  <files>mermaid-tw5/tiddlers/Requirement Diagram.tid, mermaid-tw5/tiddlers/C4 Diagram.tid</files>
  <behavior>
    - `Requirement Diagram.tid` and `C4 Diagram.tid` exist, tagged `MermaidExample`, each with a `$$$` block, ending with the catalog backlink
    - `Requirement Diagram.tid` contains a `|!Color|!Meaning|` legend matching its `classDef`/`:::` requirement-type fills (LEGEND-02); `C4 Diagram.tid` contains NO legend (D-05)
    - The extension-less bare files `Requirement Diagram` and `C4 Diagram` and their `.meta` sidecars are deleted (distinct from the new `.tid` files)
    - No file uses `<$mermaid>` body or a theme `%%{init}%%`
  </behavior>
  <read_first>
    - mermaid-tw5/tiddlers/Architecture Diagram.tid — D-01 skeleton
    - 07-PATTERNS.md §"Pattern 4" deletion table (rows for `Requirement Diagram` and `C4 Diagram` — note these bare files share the target title) and §"Pattern 3" legend table
    - 07-RESEARCH.md §"Legend Scope Matrix (D-05)" rows: Requirement Diagram = legend YES (classDef for req types), C4 Diagram = legend NO (element types labeled inline)
    - 07-RESEARCH.md §"Tiddler Inventory" rows 9 and 11 (canonical keywords `requirementDiagram` and `C4Context`)
  </read_first>
  <action>
    Create two D-01 `.tid` files. Requirement Diagram.tid: keyword `requirementDiagram`,
    real-world systems-engineering example with `requirement`/`functionalRequirement`/
    `performanceRequirement` elements, `element` blocks, and `satisfies`/`traces`
    relationships; apply `classDef`/`:::` to distinguish requirement types and add a
    matching `|!Color|!Meaning|` legend (LEGEND-02). C4 Diagram.tid: keyword `C4Context`
    (may also show `C4Container` in a second block if useful), real-world system-context
    example with `Person`/`System`/`System_Ext`/`Rel`; NO legend table (element types are
    labeled inline). Then delete the old Format B files: the extension-less
    `Requirement Diagram` and `Requirement Diagram.meta`, and the extension-less
    `C4 Diagram` and `C4 Diagram.meta`. Be precise: the file to delete has NO `.tid`
    extension; the file to keep is the new `*.tid`.
  </action>
  <verify>
    <automated>test -f "mermaid-tw5/tiddlers/Requirement Diagram.tid" && test -f "mermaid-tw5/tiddlers/C4 Diagram.tid" && grep -q 'tags: MermaidExample' "mermaid-tw5/tiddlers/Requirement Diagram.tid" && grep -q 'tags: MermaidExample' "mermaid-tw5/tiddlers/C4 Diagram.tid" && grep -q '|!Color|!Meaning|' "mermaid-tw5/tiddlers/Requirement Diagram.tid" && ! grep -q '|!Color|!Meaning|' "mermaid-tw5/tiddlers/C4 Diagram.tid" && test ! -e "mermaid-tw5/tiddlers/Requirement Diagram" && test ! -e "mermaid-tw5/tiddlers/Requirement Diagram.meta" && test ! -e "mermaid-tw5/tiddlers/C4 Diagram" && test ! -e "mermaid-tw5/tiddlers/C4 Diagram.meta" && node --test tests/phase07-structure.test.js 2>&1 | grep -qE '# fail 0'</automated>
  </verify>
  <acceptance_criteria>
    - `Requirement Diagram.tid` and `C4 Diagram.tid` exist, each tagged `MermaidExample`, each with a `$$$` block
    - `Requirement Diagram.tid` contains a `|!Color|!Meaning|` table with swatches matching its `classDef`/`style` fills; uses `requirementDiagram`
    - `C4 Diagram.tid` contains NO legend table; uses `C4Context`
    - The extension-less bare files `Requirement Diagram`, `Requirement Diagram.meta`, `C4 Diagram`, `C4 Diagram.meta` no longer exist (and the new `.tid` files do exist)
    - No file contains `<$mermaid>` or a theme `%%{init}%%`
    - `node --test tests/phase07-structure.test.js` reports `# fail 0`
  </acceptance_criteria>
  <done>Both same-title migrations complete: new `.tid` files exist with correct legend disposition, and the extension-less bare originals + `.meta` are deleted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → tiddler file | Author-controlled static `.tid` content |
| tiddler content → Mermaid renderer | Client-side render under `securityLevel: 'loose'` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | `%%{init}%%` in any migrated example | mitigate | Only structural keys permitted (D-02); enforced by `tests/phase07-structure.test.js` |
| T-07-03 | Denial of Service | stale Format B duplicate (esp. same-title bare files) left on disk | mitigate | Tasks delete bare file + `.meta`; verify asserts non-existence of the extension-less files specifically |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs — content authoring only; RESEARCH Package Legitimacy Audit "not applicable" |

No high-severity threats apply: static content authoring, no new code paths, network, auth, or runtime input. The same-title bare-file deletion is the one operational risk and is explicitly verified.
</threat_model>

<verification>
- `node --test tests/phase07-structure.test.js` reports `# fail 0`
- Full `node --test` shows no NEW failures vs baseline (27 pass / 1 pre-existing fail; plugin code unchanged)
- 5 new `.tid` files present; 10 Format B files (5 bare + 5 `.meta`) absent
- Manual: each of the 5 renders in the demo wiki without a parse error (phase-gate browser check)
</verification>

<success_criteria>
- 5 Format B canonical examples migrated to advanced Format A tiddlers (EXAMPLE-01, EXAMPLE-02)
- Requirement Diagram carries a category legend (LEGEND-02); ER, Gantt, Pie, C4 do not (D-05)
- All 5 stale Format B originals + `.meta` sidecars deleted (including the two same-title bare files)
</success_criteria>

<output>
Create `.planning/phases/07-advanced-examples-legends/07-03-SUMMARY.md` when done
</output>
