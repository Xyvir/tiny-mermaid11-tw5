---
phase: 07-advanced-examples-legends
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - mermaid-tw5/tiddlers/Flowchart.tid
  - mermaid-tw5/tiddlers/Sequence Diagram.tid
  - mermaid-tw5/tiddlers/Class Diagram.tid
  - mermaid-tw5/tiddlers/State Diagram.tid
  - mermaid-tw5/tiddlers/User Journey.tid
  - mermaid-tw5/tiddlers/Git Graph.tid
  - mermaid-tw5/tiddlers/Flowchart
  - mermaid-tw5/tiddlers/Flowchart.meta
  - mermaid-tw5/tiddlers/sequenceDiagram 1
  - mermaid-tw5/tiddlers/sequenceDiagram 1.meta
  - mermaid-tw5/tiddlers/Class diagrams
  - mermaid-tw5/tiddlers/Class diagrams.meta
  - mermaid-tw5/tiddlers/stateDiagram 1
  - mermaid-tw5/tiddlers/stateDiagram 1.meta
  - mermaid-tw5/tiddlers/User Journey Diagram
  - mermaid-tw5/tiddlers/User Journey Diagram.meta
  - mermaid-tw5/tiddlers/Gitgraph Diagram 1
  - mermaid-tw5/tiddlers/Gitgraph Diagram 1.meta
autonomous: true
requirements: [EXAMPLE-01, EXAMPLE-02, LEGEND-02]
must_haves:
  truths:
    - "Flowchart, Sequence, Class, State, User Journey, and Git Graph each have an advanced Format A example (EXAMPLE-01)"
    - "The 6 stale Format B bare files + .meta sidecars are deleted so no duplicate appears (EXAMPLE-02)"
    - "Flowchart, Class, State, User Journey, Git Graph each include a color-by-category legend (LEGEND-02)"
  artifacts:
    - path: "mermaid-tw5/tiddlers/Flowchart.tid"
      provides: "Advanced flowchart example with classDef legend"
      contains: "flowchart"
    - path: "mermaid-tw5/tiddlers/Sequence Diagram.tid"
      provides: "Advanced sequenceDiagram example (no legend per D-05)"
      contains: "sequenceDiagram"
    - path: "mermaid-tw5/tiddlers/Class Diagram.tid"
      provides: "Advanced classDiagram example with legend"
      contains: "classDiagram"
    - path: "mermaid-tw5/tiddlers/State Diagram.tid"
      provides: "Advanced stateDiagram-v2 example with legend"
      contains: "stateDiagram-v2"
    - path: "mermaid-tw5/tiddlers/User Journey.tid"
      provides: "Advanced journey example with legend"
      contains: "journey"
    - path: "mermaid-tw5/tiddlers/Git Graph.tid"
      provides: "Advanced gitGraph example with branch-color legend"
      contains: "gitGraph"
  key_links:
    - from: "mermaid-tw5/tiddlers/Flowchart.tid"
      to: "Mermaid Chart Catalog"
      via: "[[← Mermaid Chart Catalog]] backlink"
      pattern: "Mermaid Chart Catalog"
---

<objective>
Migrate the first 6 Format B canonical examples (Flowchart, Sequence, Class, State,
User Journey, Git Graph) into fresh Format A `.tid` tiddlers built on the D-01 template,
and delete the stale Format B originals + `.meta` sidecars. Five of the six color by
category and get a WikiText legend table (LEGEND-02).

Purpose: EXAMPLE-01 requires every in-scope type to have an advanced, well-commented
`$$$`-block example; EXAMPLE-02 requires the older bare-content tiddlers to be migrated
to consistent `.tid`. The old Format B diagram source is discarded — write new advanced
examples per the D-01 template (per PATTERNS Pattern 4).
Output: 6 new `.tid` files; 12 deleted Format B files (6 bare + 6 `.meta`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-advanced-examples-legends/07-CONTEXT.md
@.planning/phases/07-advanced-examples-legends/07-RESEARCH.md
@.planning/phases/07-advanced-examples-legends/07-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author Flowchart, Sequence Diagram, and Class Diagram (Format B → A migration)</name>
  <files>mermaid-tw5/tiddlers/Flowchart.tid, mermaid-tw5/tiddlers/Sequence Diagram.tid, mermaid-tw5/tiddlers/Class Diagram.tid</files>
  <behavior>
    - Each new `.tid` exists, is tagged `MermaidExample`, contains a `$$$text/vnd.tiddlywiki.mermaid` block, and ends with `[[← Mermaid Chart Catalog]]`
    - Flowchart and Class Diagram contain a `|!Color|!Meaning|` legend table with swatch colors matching their `classDef`/`:::` fills (LEGEND-02); Sequence Diagram contains NO legend (D-05)
    - Old Format B files `Flowchart`, `sequenceDiagram 1`, `Class diagrams` and their `.meta` sidecars are deleted
    - No file contains `<$mermaid>` body or a `%%{init}%%` with theme/look/fontFamily/themeVariables keys
  </behavior>
  <read_first>
    - mermaid-tw5/tiddlers/Architecture Diagram.tid — exact D-01 skeleton to replicate (front-matter, sections, `%%` banner style, backlink)
    - 07-PATTERNS.md §"Pattern 2: %% Section Banner Comments" (the flowchart classDef example is the model) and §"Pattern 3: WikiText Table Legend" and §"Pattern 4: Format B Migration Structure" (the deletion table)
    - 07-RESEARCH.md §"Pitfall 6" rows for Flowchart (`quote "End State"`), classDiagram (no nested generics `~Map<K,V>~`) and §"State of the Art" (use `flowchart`/`classDiagram`/`sequenceDiagram` canonical keywords)
    - 07-RESEARCH.md §"Per-Type Structural %%{init}%% Usage" — Flowchart `{"flowchart":{"curve":"monotoneX"}}`, Sequence `{"sequence":{"showSequenceNumbers":true}}`
  </read_first>
  <action>
    Create three `.tid` files with front-matter (`created`/`modified: 20260609000000000`,
    `type: text/vnd.tiddlywiki`, `tags: MermaidExample`) and the D-01 body
    (intro → `!! Basic example` fenced+`$$$` pair → `!! Real-world example: <subtitle>`
    fenced+`$$$` pair with `%% ── section ──` banners → legend table where applicable →
    `!! Syntax` → `!! Tips` → `[[← Mermaid Chart Catalog]]`).
    Flowchart.tid: keyword `flowchart TD`, a CI/CD or order-processing real-world example
    using `classDef` categories, optional `%%{init}: {"flowchart": {"curve": "monotoneX"}}`,
    plus a `|!Color|!Meaning|` legend whose `@@background-color:#...;@@` swatches equal the
    `classDef ... fill:#...` values. Quote any node labeled "end" as `["End State"]`.
    Sequence Diagram.tid: keyword `sequenceDiagram`, real-world API/auth flow with
    `participant`/`activate`/`alt`/`loop`, optional `%%{init}: {"sequence": {"showSequenceNumbers": true}}`,
    NO legend table (D-05). Class Diagram.tid: keyword `classDiagram`, real-world domain
    model with visibility markers and `:::`/`classDef` stereotype categories and a matching
    legend table; use only simple generics `~Map~` (never `~Map<K,V>~`).
    Then delete the old Format B files: `Flowchart`, `Flowchart.meta`, `sequenceDiagram 1`,
    `sequenceDiagram 1.meta`, `Class diagrams`, `Class diagrams.meta`.
  </action>
  <verify>
    <automated>for f in "Flowchart" "Sequence Diagram" "Class Diagram"; do test -f "mermaid-tw5/tiddlers/$f.tid" && grep -q 'tags: MermaidExample' "mermaid-tw5/tiddlers/$f.tid" && grep -q 'text/vnd.tiddlywiki.mermaid' "mermaid-tw5/tiddlers/$f.tid" || exit 1; done; test ! -e "mermaid-tw5/tiddlers/Flowchart" && test ! -e "mermaid-tw5/tiddlers/sequenceDiagram 1" && test ! -e "mermaid-tw5/tiddlers/Class diagrams" && grep -q '|!Color|!Meaning|' "mermaid-tw5/tiddlers/Flowchart.tid" && node --test tests/phase07-structure.test.js 2>&1 | grep -qE '# fail 0'</automated>
  </verify>
  <acceptance_criteria>
    - `Flowchart.tid`, `Sequence Diagram.tid`, `Class Diagram.tid` exist, each tagged `MermaidExample`, each with a `$$$text/vnd.tiddlywiki.mermaid` block
    - `Flowchart.tid` and `Class Diagram.tid` each contain a `|!Color|!Meaning|` table with `@@background-color:#...` swatches matching `classDef`/`style` `fill:#...` values
    - `Sequence Diagram.tid` contains NO `|!Color|!Meaning|` legend table
    - `Flowchart.tid` uses `flowchart` (not `graph`); `Class Diagram.tid` uses `classDiagram` (not `classDiagram-v2`)
    - No file contains `<$mermaid>` or `%%{init}` with theme/look/fontFamily/themeVariables
    - Old files `Flowchart`, `Flowchart.meta`, `sequenceDiagram 1`, `sequenceDiagram 1.meta`, `Class diagrams`, `Class diagrams.meta` no longer exist
    - `node --test tests/phase07-structure.test.js` reports `# fail 0`
  </acceptance_criteria>
  <done>Three migrated tiddlers exist with advanced examples and legends where required; the three Format B originals are gone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author State Diagram, User Journey, and Git Graph (Format B → A migration)</name>
  <files>mermaid-tw5/tiddlers/State Diagram.tid, mermaid-tw5/tiddlers/User Journey.tid, mermaid-tw5/tiddlers/Git Graph.tid</files>
  <behavior>
    - Each new `.tid` exists, is tagged `MermaidExample`, contains a `$$$` mermaid block, ends with the catalog backlink
    - All three color by category and contain a `|!Color|!Meaning|` legend table (LEGEND-02): State via `classDef`, User Journey via section/actor colors, Git Graph via branch colors
    - Old Format B files `stateDiagram 1`, `User Journey Diagram`, `Gitgraph Diagram 1` and their `.meta` sidecars are deleted
    - No file uses `<$mermaid>` body or a theme `%%{init}%%`
  </behavior>
  <read_first>
    - mermaid-tw5/tiddlers/Architecture Diagram.tid — D-01 skeleton
    - 07-PATTERNS.md §"Pattern 3: WikiText Table Legend" (full-color and simpler text-label variants) and §"Pattern 4" deletion table
    - 07-RESEARCH.md §"Pitfall 6" rows for stateDiagram-v2 (avoid `:` in state labels) and §"State of the Art" (`stateDiagram-v2`, `gitGraph`, `journey` canonical keywords)
    - 07-RESEARCH.md §"Legend Scope Matrix (D-05)" rows for State Diagram, User Journey, Git Graph (Git Graph legend describes branch colors `git0`–`git7`)
  </read_first>
  <action>
    Create three D-01 `.tid` files (same front-matter and section structure as Task 1).
    State Diagram.tid: keyword `stateDiagram-v2`, real-world lifecycle (e.g. order or
    document state machine) with `classDef` distinguishing state types and a matching
    `|!Color|!Meaning|` legend; avoid `:` inside state labels. User Journey.tid: keyword
    `journey`, real-world customer-experience journey with multiple `section`s; add a
    legend table mapping the cycled section colors to actor/phase meaning (use the simpler
    text-label legend variant `|!Section|!Color|!Meaning|` since journey section colors
    cycle automatically). Git Graph.tid: keyword `gitGraph`, real-world release-flow with
    `branch`/`checkout`/`commit`/`merge`/tags; add a legend mapping branch names to their
    branch colors (describe `git0`–`git7` order). Then delete the old Format B files:
    `stateDiagram 1`, `stateDiagram 1.meta`, `User Journey Diagram`,
    `User Journey Diagram.meta`, `Gitgraph Diagram 1`, `Gitgraph Diagram 1.meta`.
  </action>
  <verify>
    <automated>for f in "State Diagram" "User Journey" "Git Graph"; do test -f "mermaid-tw5/tiddlers/$f.tid" && grep -q 'tags: MermaidExample' "mermaid-tw5/tiddlers/$f.tid" && grep -q 'text/vnd.tiddlywiki.mermaid' "mermaid-tw5/tiddlers/$f.tid" && grep -qE '\|!.*\|!' "mermaid-tw5/tiddlers/$f.tid" || exit 1; done; test ! -e "mermaid-tw5/tiddlers/stateDiagram 1" && test ! -e "mermaid-tw5/tiddlers/User Journey Diagram" && test ! -e "mermaid-tw5/tiddlers/Gitgraph Diagram 1" && node --test tests/phase07-structure.test.js 2>&1 | grep -qE '# fail 0'</automated>
  </verify>
  <acceptance_criteria>
    - `State Diagram.tid`, `User Journey.tid`, `Git Graph.tid` exist, each tagged `MermaidExample`, each with a `$$$` mermaid block
    - Each of the three contains a WikiText legend table (`|!Color|!Meaning|` or `|!Section|!Color|!Meaning|`)
    - `State Diagram.tid` uses `stateDiagram-v2`; `Git Graph.tid` uses `gitGraph`; `User Journey.tid` uses `journey`
    - No `:` appears inside any state label in `State Diagram.tid`
    - No file contains `<$mermaid>` or a theme `%%{init}%%`
    - Old files `stateDiagram 1`(+.meta), `User Journey Diagram`(+.meta), `Gitgraph Diagram 1`(+.meta) no longer exist
    - `node --test tests/phase07-structure.test.js` reports `# fail 0`
  </acceptance_criteria>
  <done>Three migrated tiddlers exist with advanced examples and legends; the three Format B originals are gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → tiddler file | Author-controlled static `.tid` content |
| tiddler content → Mermaid renderer | Client-side render under `securityLevel: 'loose'` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | `%%{init}%%` in any migrated example | mitigate | Only structural keys permitted (D-02); enforced by `tests/phase07-structure.test.js` theme-directive assertion run in each task verify |
| T-07-03 | Denial of Service | stale Format B duplicate left on disk | mitigate | Tasks delete both bare file and `.meta` sidecar (Pitfall 4); verify asserts non-existence |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs — content authoring only; RESEARCH Package Legitimacy Audit "not applicable" |

No high-severity threats apply: static content authoring with no new code paths, network, auth, or runtime input. The realistic risk (a content directive overriding the user's global theme, or a duplicate stale tiddler) is mitigated by the D-02 lint and the deletion verification.
</threat_model>

<verification>
- `node --test tests/phase07-structure.test.js` reports `# fail 0`
- Full `node --test` shows no NEW failures vs baseline (27 pass / 1 pre-existing fail in `wrapper.test.js`; plugin code unchanged)
- 6 new `.tid` files present; 12 Format B files (6 bare + 6 `.meta`) absent
- Manual: each of the 6 renders in the demo wiki without a parse error (phase-gate browser check, deferred to phase verification)
</verification>

<success_criteria>
- 6 Format B canonical examples migrated to advanced Format A tiddlers (EXAMPLE-01, EXAMPLE-02)
- Flowchart, Class, State, User Journey, Git Graph each carry a category legend (LEGEND-02); Sequence has none (D-05)
- All 6 stale Format B originals + `.meta` sidecars deleted
</success_criteria>

<output>
Create `.planning/phases/07-advanced-examples-legends/07-02-SUMMARY.md` when done
</output>
