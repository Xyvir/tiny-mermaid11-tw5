---
phase: 07-advanced-examples-legends
plan: 05
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - mermaid-tw5/tiddlers/Radar Chart.tid
  - mermaid-tw5/tiddlers/Venn Diagram.tid
  - mermaid-tw5/tiddlers/Ishikawa Diagram.tid
  - mermaid-tw5/tiddlers/Treemap.tid
  - mermaid-tw5/tiddlers/Tree View.tid
  - mermaid-tw5/tiddlers/Wardley Map.tid
autonomous: true
requirements: [EXAMPLE-01, LEGEND-02]
must_haves:
  truths:
    - "All 6 net-new beta-type tiddlers exist as Format A with the MermaidExample tag and a %%-commented advanced example (EXAMPLE-01)"
    - "Venn Diagram and Treemap include a color-by-category legend; Radar, Ishikawa, Tree View, Wardley do not (LEGEND-02 / D-05)"
    - "Beta-type syntax matches the verified RESEARCH grammar; the two MEDIUM-confidence edge cases (wardley annotation, venn intersection text) are verified before commit"
  artifacts:
    - path: "mermaid-tw5/tiddlers/Radar Chart.tid"
      provides: "radar-beta example with native legend"
      contains: "radar-beta"
    - path: "mermaid-tw5/tiddlers/Venn Diagram.tid"
      provides: "venn-beta example with set-color legend"
      contains: "venn-beta"
    - path: "mermaid-tw5/tiddlers/Ishikawa Diagram.tid"
      provides: "ishikawa-beta fishbone example"
      contains: "ishikawa-beta"
    - path: "mermaid-tw5/tiddlers/Treemap.tid"
      provides: "treemap-beta example with section-color legend"
      contains: "treemap-beta"
    - path: "mermaid-tw5/tiddlers/Tree View.tid"
      provides: "treeView-beta hierarchy example"
      contains: "treeView-beta"
    - path: "mermaid-tw5/tiddlers/Wardley Map.tid"
      provides: "wardley-beta strategy map example"
      contains: "wardley-beta"
  key_links:
    - from: "mermaid-tw5/tiddlers/Radar Chart.tid"
      to: "Mermaid Chart Catalog"
      via: "[[← Mermaid Chart Catalog]] backlink"
      pattern: "Mermaid Chart Catalog"
---

<objective>
Create the 6 net-new beta-type example tiddlers (Radar, Venn, Ishikawa, Treemap,
Tree View, Wardley) as Format A `.tid` files on the D-01 template, using the
RESEARCH-verified beta-type syntax. Venn and Treemap color by category and get a legend.

Purpose: these 6 types (radar-beta, venn-beta, ishikawa-beta, treemap-beta,
treeView-beta, wardley-beta) have no existing tiddler — EXAMPLE-01 requires an advanced
example for each. Syntax is verified in RESEARCH against the vendored 11.14.0 grammar,
with two MEDIUM-confidence edge cases (wardley `annotation`, venn intersection `text`)
that must be confirmed in the demo wiki before committing those constructs.
Output: 6 new `.tid` files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-advanced-examples-legends/07-CONTEXT.md
@.planning/phases/07-advanced-examples-legends/07-RESEARCH.md
@.planning/phases/07-advanced-examples-legends/07-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Radar Chart, Treemap, Tree View</name>
  <files>mermaid-tw5/tiddlers/Radar Chart.tid, mermaid-tw5/tiddlers/Treemap.tid, mermaid-tw5/tiddlers/Tree View.tid</files>
  <read_first>
    - mermaid-tw5/tiddlers/Architecture Diagram.tid — D-01 skeleton (these are net-new, copy structure exactly)
    - 07-PATTERNS.md §"Beta Type Syntax Excerpts" rows for `radar-beta`, `treemap-beta`, `treeView-beta` (the source-of-truth minimal examples) and §"Pattern 3" (legend for Treemap)
    - 07-RESEARCH.md §"Beta Type Syntax Reference" §1 (treeView-beta — camelCase V required, trailing `/` = directory, extension icons) and §4 (treemap-beta — leaf nodes need `: value`, parents do not, use "R and D" not "R&D") and §"Per-Type Structural %%{init}%%" (radar width/height, treemap `showValues`, treeView `rowIndent`)
    - 07-RESEARCH.md §"Legend Scope Matrix (D-05)": Radar SKIP (native `showLegend`), Treemap YES (section colors), Tree View NO
  </read_first>
  <action>
    Create three D-01 `.tid` files (front-matter `created`/`modified: 20260609000000000`,
    `type: text/vnd.tiddlywiki`, `tags: MermaidExample`; full template body). Radar Chart:
    keyword `radar-beta` (beta suffix is REQUIRED — no plain `radar`), real-world
    multi-axis comparison (e.g. team skills or product comparison) with `axis`/`curve` and
    multiple data series whose value counts exactly match the axis count; optional
    `%%{init}: {"radar": {"width": 500, "height": 500}}`; NO WikiText legend (radar has a
    native legend — note `showLegend` in Tips). Treemap: keyword `treemap-beta`, real-world
    hierarchical budget/allocation with `title`, quoted section parents and `"Leaf": N`
    leaves (positive numbers only; never `&`, use "R and D" if needed); ADD a
    `|!Section|!Color|!Meaning|` legend mapping section colors to categories (LEGEND-02).
    Tree View: keyword `treeView-beta` (exact camelCase V), real-world project/file tree
    using trailing `/` for directories and known extensions (`.ts`/`.json`/`.md` etc.) for
    auto-icons; SPACES for indentation; optional `%%{init}: {"treeView": {"rowIndent": 20}}`;
    NO legend. End each with `[[← Mermaid Chart Catalog]]`.
  </action>
  <verify>
    <automated>grep -q 'radar-beta' "mermaid-tw5/tiddlers/Radar Chart.tid" && grep -q 'treemap-beta' "mermaid-tw5/tiddlers/Treemap.tid" && grep -q 'treeView-beta' "mermaid-tw5/tiddlers/Tree View.tid" && for f in "Radar Chart" "Treemap" "Tree View"; do grep -q 'tags: MermaidExample' "mermaid-tw5/tiddlers/$f.tid" || exit 1; done; grep -qE '\|!.*\|!' "mermaid-tw5/tiddlers/Treemap.tid" && ! grep -q '|!Color|!Meaning|' "mermaid-tw5/tiddlers/Radar Chart.tid" && node --test tests/phase07-structure.test.js 2>&1 | grep -qE '# fail 0'</automated>
  </verify>
  <acceptance_criteria>
    - `Radar Chart.tid` uses `radar-beta` (with beta suffix); `Treemap.tid` uses `treemap-beta`; `Tree View.tid` uses `treeView-beta` (exact camelCase V, never `treeview-beta`)
    - All three tagged `MermaidExample`, each with a `$$$` block and the catalog backlink
    - `Treemap.tid` contains a legend table; `Radar Chart.tid` and `Tree View.tid` do NOT
    - `Treemap.tid` leaf nodes have `: <positive number>`; no `&` anywhere
    - Radar series value counts equal the `axis` count
    - No file contains `<$mermaid>` or a theme `%%{init}%%`
    - `node --test tests/phase07-structure.test.js` reports `# fail 0`
  </acceptance_criteria>
  <done>Radar, Treemap, Tree View created with verified beta syntax; Treemap has a legend, Radar relies on its native legend.</done>
</task>

<task type="auto">
  <name>Task 2: Create Venn Diagram, Ishikawa Diagram, Wardley Map (with edge-case verification)</name>
  <files>mermaid-tw5/tiddlers/Venn Diagram.tid, mermaid-tw5/tiddlers/Ishikawa Diagram.tid, mermaid-tw5/tiddlers/Wardley Map.tid</files>
  <read_first>
    - mermaid-tw5/tiddlers/Architecture Diagram.tid — D-01 skeleton
    - 07-PATTERNS.md §"Beta Type Syntax Excerpts" rows for `venn-beta`, `ishikawa-beta`, `wardley-beta` and §"Pattern 3" (legend for Venn)
    - 07-RESEARCH.md §"Beta Type Syntax Reference" §2 (wardley-beta — position is `[visibility, evolution]` Y-first; `annotation` is LOW-confidence) §3 (ishikawa-beta — first line = effect, indented = cause categories) §5 (venn-beta — `set` before `union`, no forward refs; intersection `text` placement is MEDIUM-confidence)
    - 07-RESEARCH.md §"Open Questions" 2 (wardley `pipeline` — omit if unverified) and 3 (venn intersection label — place text only in exclusive regions); §"Assumptions Log" A3 (wardley annotation) and A5 (venn style)
    - 07-RESEARCH.md §"Legend Scope Matrix (D-05)": Venn YES (`style fill:`), Ishikawa NO, Wardley NO
  </read_first>
  <action>
    Create three D-01 `.tid` files. Venn Diagram: keyword `venn-beta`, real-world skills/
    responsibility overlap; declare every `set` BEFORE any `union` (no forward references);
    use `style ID fill:#color` to color sets and add a `|!Color|!Meaning|` legend matching
    those fills (LEGEND-02); place `text` blocks only in exclusive set regions and note the
    intersection-text limitation in Tips; optional `%%{init}: {"venn": {"width": 600,
    "height": 400}}`. Ishikawa Diagram: keyword `ishikawa-beta`, real-world root-cause
    (effect on line 1, then indented cause categories and sub-causes); NO legend; no
    `%%{init}%%` needed. Wardley Map: keyword `wardley-beta`, real-world strategy map with
    `title`/`anchor`/`component [visibility, evolution]` (Y first, X second) and `->`
    dependency links plus build/buy/outsource decorators; NO legend. For the two
    MEDIUM/LOW-confidence constructs — wardley `annotation`/`pipeline` and venn intersection
    `text` — render the candidate diagram in the demo wiki FIRST; if it does not render
    cleanly, OMIT that construct and keep the simpler verified form (do not commit a
    non-rendering construct). End each with `[[← Mermaid Chart Catalog]]`.
  </action>
  <verify>
    <automated>grep -q 'venn-beta' "mermaid-tw5/tiddlers/Venn Diagram.tid" && grep -q 'ishikawa-beta' "mermaid-tw5/tiddlers/Ishikawa Diagram.tid" && grep -q 'wardley-beta' "mermaid-tw5/tiddlers/Wardley Map.tid" && for f in "Venn Diagram" "Ishikawa Diagram" "Wardley Map"; do grep -q 'tags: MermaidExample' "mermaid-tw5/tiddlers/$f.tid" || exit 1; done; grep -q '|!Color|!Meaning|' "mermaid-tw5/tiddlers/Venn Diagram.tid" && ! grep -q '|!Color|!Meaning|' "mermaid-tw5/tiddlers/Ishikawa Diagram.tid" && ! grep -q '|!Color|!Meaning|' "mermaid-tw5/tiddlers/Wardley Map.tid" && node --test tests/phase07-structure.test.js 2>&1 | grep -qE '# fail 0'</automated>
  </verify>
  <acceptance_criteria>
    - `Venn Diagram.tid` uses `venn-beta`; `Ishikawa Diagram.tid` uses `ishikawa-beta`; `Wardley Map.tid` uses `wardley-beta`
    - All three tagged `MermaidExample`, each with a `$$$` block and catalog backlink
    - `Venn Diagram.tid` contains a `|!Color|!Meaning|` table matching its `style ... fill:#...` values; declares all `set`s before any `union`; `Ishikawa Diagram.tid` and `Wardley Map.tid` contain NO legend
    - Wardley positions are written `[visibility, evolution]` (Y first); any `annotation`/`pipeline` present has been confirmed to render in the demo wiki, otherwise omitted
    - Venn `text` blocks sit in exclusive set regions; intersection-text limitation noted in Tips
    - No file contains `<$mermaid>` or a theme `%%{init}%%`
    - `node --test tests/phase07-structure.test.js` reports `# fail 0`
  </acceptance_criteria>
  <done>Venn, Ishikawa, Wardley created; edge-case constructs verified-or-omitted; Venn has a legend.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → tiddler file | Author-controlled static `.tid` content |
| tiddler content → Mermaid renderer | Client-side render under `securityLevel: 'loose'` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | `%%{init}%%` in any beta example | mitigate | Only structural keys permitted (D-02); enforced by `tests/phase07-structure.test.js` |
| T-07-06 | Denial of Service | non-rendering beta construct committed (wardley annotation/pipeline, venn intersection text) | mitigate | Task 2 requires demo-wiki render confirmation before committing those MEDIUM/LOW-confidence constructs; omit if they fail |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs — content authoring only; RESEARCH Package Legitimacy Audit "not applicable" |

No high-severity threats apply: static content authoring with no new code paths, network, auth, or runtime input. The beta-syntax render risk is operational (a broken example), not a security threat, and is mitigated by verify-before-commit.
</threat_model>

<verification>
- `node --test tests/phase07-structure.test.js` reports `# fail 0`
- Full `node --test` shows no NEW failures vs baseline (27 pass / 1 pre-existing fail; plugin code unchanged)
- All 6 new `.tid` files present with the correct beta keyword and the `MermaidExample` tag
- Manual (REQUIRED before commit for the 6 beta types): each renders without a parse error in the demo wiki; the two MEDIUM-confidence constructs are either confirmed or omitted
</verification>

<success_criteria>
- 6 net-new beta-type tiddlers created with verified syntax and advanced examples (EXAMPLE-01)
- Venn and Treemap carry category legends (LEGEND-02); Radar uses its native legend; Ishikawa/Tree View/Wardley need none (D-05)
- No non-rendering beta construct committed
</success_criteria>

<output>
Create `.planning/phases/07-advanced-examples-legends/07-05-SUMMARY.md` when done
</output>
