---
phase: 07-advanced-examples-legends
plan: 06
type: execute
wave: 3
depends_on: ["07-02", "07-03", "07-04", "07-05"]
files_modified:
  - mermaid-tw5/tiddlers/Mermaid Chart Catalog.tid
autonomous: true
requirements: [EXAMPLE-03]
must_haves:
  truths:
    - "Every advanced example is reachable from the curated Mermaid Chart Catalog and from the auto-index (EXAMPLE-03)"
    - "Catalog links point to the new clean titles, not the migrated-away informal titles (no dead links)"
    - "The 6 new types and the Legend Recipe are linked; the stale 'Twenty diagram types' wording is corrected to 26"
  artifacts:
    - path: "mermaid-tw5/tiddlers/Mermaid Chart Catalog.tid"
      provides: "Updated curated catalog + auto-index for all 26 examples + legend recipe"
      contains: "list-links filter:[tag[MermaidExample]]"
  key_links:
    - from: "mermaid-tw5/tiddlers/Mermaid Chart Catalog.tid"
      to: "tag[MermaidExample]"
      via: "<<list-links>> auto-index"
      pattern: "list-links filter"
---

<objective>
Update the hand-curated `Mermaid Chart Catalog.tid` (D-10): correct the type count to 26,
repoint the migrated links to their new clean titles, add the 6 new types under their
recommended categories, link the Legend Recipe, and add a `<<list-links>>` auto-index
over `[tag[MermaidExample]]` as a safety net. Then run the phase-completion gate so the
full 26 + legend-recipe set is asserted present and lint-clean.

Purpose: EXAMPLE-03 requires every advanced example to be discoverable via a shared tag
AND a catalog/index. This plan is wave 3 because the auto-index and the repointed links
require all 26 example tiddlers (and the legend recipe) to already exist.
Output: 1 modified catalog `.tid` file; phase-completion verification run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-advanced-examples-legends/07-CONTEXT.md
@.planning/phases/07-advanced-examples-legends/07-RESEARCH.md
@.planning/phases/07-advanced-examples-legends/07-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update the Mermaid Chart Catalog (D-10) and link the Legend Recipe</name>
  <files>mermaid-tw5/tiddlers/Mermaid Chart Catalog.tid</files>
  <read_first>
    - mermaid-tw5/tiddlers/Mermaid Chart Catalog.tid — the file being modified (its current category tables, the "Twenty diagram types" intro, and the "See also" footer)
    - 07-PATTERNS.md §"mermaid-tw5/tiddlers/Mermaid Chart Catalog.tid (catalog-index, modify)" — the exact 5 changes required (count wording, link repoints, 6 new-type rows + categories, legend link, auto-index)
    - 07-RESEARCH.md §"Pattern 4: Catalog Update (D-10)" — category placement table for the 6 new types and the auto-index snippet
  </read_first>
  <action>
    Edit the catalog in place. (1) Change the intro sentence "Twenty diagram types are
    available — nine of them (marked ''New'')..." to "Twenty-six diagram types are
    available." (2) Repoint the migrated links to their new clean titles:
    `[[sequenceDiagram 1|sequenceDiagram 1]]`→`[[Sequence Diagram]]`,
    `[[User Journey Diagram]]`→`[[User Journey]]`, `[[Pie chart diagrams]]`→`[[Pie Chart]]`,
    `[[Class diagrams]]`→`[[Class Diagram]]`, `[[Defining Relationship]]`→`[[Entity Relationship]]`,
    `[[stateDiagram 1]]`→`[[State Diagram]]`, `[[Gitgraph Diagram 1]]`→`[[Git Graph]]`,
    `[[Gantt 1]]`→`[[Gantt]]`; leave `[[Flowchart]]`, `[[Requirement Diagram]]`,
    `[[C4 Diagram]]`, `[[Architecture Diagram]]` as-is (titles unchanged). (3) Add 6 new
    rows under the recommended categories: `[[Radar Chart]]` and `[[Treemap]]` under
    "Data and Metrics"; `[[Wardley Map]]` under "Architecture and Systems";
    `[[Venn Diagram]]`, `[[Ishikawa Diagram]]`, `[[Tree View]]` under "Exploration and
    Thinking" — each with a one-line "Best for" cell. (4) Add `· [[Mermaid Legend Recipe]]`
    to the "See also" footer line. (5) Add a new section at the end (keeping the footer):
    a `!! All examples` heading followed by `<<list-links filter:"[tag[MermaidExample]]">>`.
    Optionally drop the now-inaccurate `''New''` markers. Bump `modified` to
    `20260609000000000`. Do NOT leave any link pointing at a migrated-away informal title.
  </action>
  <verify>
    <automated>bash -c 'F="mermaid-tw5/tiddlers/Mermaid Chart Catalog.tid"; grep -q "Twenty-six diagram types are available" "$F" && grep -q "list-links filter" "$F" && grep -q "MermaidExample" "$F" && grep -q "\[\[Mermaid Legend Recipe\]\]" "$F" && for n in "Sequence Diagram" "User Journey" "Pie Chart" "Class Diagram" "Entity Relationship" "State Diagram" "Git Graph" "Gantt" "Radar Chart" "Treemap" "Wardley Map" "Venn Diagram" "Ishikawa Diagram" "Tree View"; do grep -q "\[\[$n\]\]" "$F" || exit 1; done && ! grep -qE "\[\[(sequenceDiagram 1|User Journey Diagram|Pie chart diagrams|Class diagrams|Defining Relationship|stateDiagram 1|Gitgraph Diagram 1|Gantt 1)" "$F"'</automated>
  </verify>
  <acceptance_criteria>
    - Catalog intro reads "Twenty-six diagram types are available."
    - Catalog contains a `<<list-links filter:"[tag[MermaidExample]]">>` auto-index
    - Catalog links to `[[Mermaid Legend Recipe]]`
    - Catalog contains wikilinks to all 14 retitled/new types: Sequence Diagram, User Journey, Pie Chart, Class Diagram, Entity Relationship, State Diagram, Git Graph, Gantt, Radar Chart, Treemap, Wardley Map, Venn Diagram, Ishikawa Diagram, Tree View
    - Catalog contains NO link to any migrated-away informal title (`sequenceDiagram 1`, `User Journey Diagram`, `Pie chart diagrams`, `Class diagrams`, `Defining Relationship`, `stateDiagram 1`, `Gitgraph Diagram 1`, `Gantt 1`)
  </acceptance_criteria>
  <done>The catalog reflects all 26 types with clean-title links, links the legend recipe, and includes the auto-index safety net.</done>
</task>

<task type="auto">
  <name>Task 2: Run the phase-completion gate (all 26 + recipe present and lint-clean)</name>
  <files>tests/phase07-structure.test.js</files>
  <read_first>
    - tests/phase07-structure.test.js — the structural test created in Plan 01 (note the `PHASE07_COMPLETE` env flag that hard-asserts the full 26 + recipe set)
    - 07-RESEARCH.md §"Phase Requirements → Test Map" — the full structural assertion set this gate exercises
  </read_first>
  <action>
    Run the structural suite with the completion flag set so the full-presence gate is
    enforced: `PHASE07_COMPLETE=1 node --test tests/phase07-structure.test.js`. Confirm
    every one of the 26 example titles plus `Mermaid Legend Recipe` exists, every example
    is tagged `MermaidExample`, no example uses a `<$mermaid>` body or a theme `%%{init}%%`,
    every example contains a `$$$` block, and Sankey contains no `&`. Also run the full
    suite `node --test` and confirm no NEW failures relative to the phase baseline (baseline
    is 27 pass / 1 pre-existing fail in `wrapper.test.js` "displays a friendly error message
    for invalid syntax", last touched commit ea71426; plugin code is unchanged this phase,
    so that one failure may remain — it is NOT a Phase 7 regression). If the completion gate
    reports any missing or non-compliant tiddler, do not mark the phase complete; report
    which file fails and stop.
  </action>
  <verify>
    <automated>PHASE07_COMPLETE=1 node --test tests/phase07-structure.test.js 2>&1 | grep -qE '# fail 0'</automated>
  </verify>
  <acceptance_criteria>
    - `PHASE07_COMPLETE=1 node --test tests/phase07-structure.test.js` reports `# fail 0`
    - All 26 example `.tid` files plus `Mermaid Legend Recipe.tid` exist
    - Full `node --test` shows no NEW failure beyond the documented pre-existing `wrapper.test.js` case
  </acceptance_criteria>
  <done>The completion gate is green: all 26 examples + recipe present, tagged, and lint-clean; no Phase 7 regression in the full suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → catalog file | Author-controlled static `.tid` content |
| catalog `<<list-links>>` → tag index | Built-in TW5 macro filtering `[tag[MermaidExample]]`; no plugin logic, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Denial of Service | dead catalog link to a migrated-away informal title | mitigate | Task 1 repoints all migrated links; verify greps assert the old informal titles are absent and the new titles present |
| T-07-08 | Spoofing | example silently missing from discovery (untagged) | mitigate | `<<list-links>>` auto-index + completion-gate test assert every example carries `MermaidExample` and is reachable |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs — content edit + running the existing `node:test` suite; RESEARCH Package Legitimacy Audit "not applicable" |

No high-severity threats apply: this plan edits one static catalog file and runs the existing test suite, with no new code paths, network, auth, or runtime input. The realistic risks (dead links, undiscoverable examples) are mitigated by the link-repoint verification and the auto-index + completion gate.
</threat_model>

<verification>
- Task 1 catalog grep assertions pass (count wording, auto-index, legend link, all new titles present, no informal titles)
- `PHASE07_COMPLETE=1 node --test tests/phase07-structure.test.js` reports `# fail 0`
- Full `node --test` shows no NEW failures vs baseline (27 pass / 1 pre-existing fail; plugin code unchanged)
- Manual phase gate: catalog renders in the demo wiki, the auto-index lists all 27 MermaidExample tiddlers, and every curated link resolves (no missing-tiddler styling)
</verification>

<success_criteria>
- Catalog updated to 26 types with clean-title links, legend-recipe link, and a `<<list-links>>` auto-index (EXAMPLE-03 / D-10)
- No dead links to migrated-away titles
- Phase-completion gate green: all 26 examples + recipe present, tagged, lint-clean; no Phase 7 regression
</success_criteria>

<output>
Create `.planning/phases/07-advanced-examples-legends/07-06-SUMMARY.md` when done
</output>
