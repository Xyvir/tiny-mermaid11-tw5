---
phase: 06-config-wiring-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/helpers/tw-bootstrap.js
  - mermaid-tw5/plugins/mermaid-tw5/$__plugins_mermaid-tw5_config.tid
  - mermaid-tw5/plugins/mermaid-tw5/plugin.info
  - mermaid-tw5/tiddlers/Sankey Diagram.tid
autonomous: true
requirements: [CONFIG-04, CONFIG-06]
must_haves:
  truths:
    - "Test mock records every config object passed to mermaidAPI.initialize()"
    - "Test mock records the last scriptBody passed to mermaidAPI.render()"
    - "A JSON shadow config tiddler $:/plugins/orange/mermaid-tw5/config ships in the plugin bundle"
    - "The Sankey example renders without an R&D parse error"
    - "The 14 currently-passing tests stay green"
  artifacts:
    - path: "tests/helpers/tw-bootstrap.js"
      provides: "initializeCalls + lastRenderSource tracking on mockMermaidAPI"
      contains: "initializeCalls"
    - path: "mermaid-tw5/plugins/mermaid-tw5/$__plugins_mermaid-tw5_config.tid"
      provides: "Site-wide Mermaid default config (securityLevel loose, theme, look, fontFamily, flowchart block)"
      contains: "application/json"
    - path: "mermaid-tw5/plugins/mermaid-tw5/plugin.info"
      provides: "config tiddler registered in plugin list field"
      contains: "config"
    - path: "mermaid-tw5/tiddlers/Sankey Diagram.tid"
      provides: "Sankey example with valid node names"
  key_links:
    - from: "buildSiteConfig() (Plan 02)"
      to: "$:/plugins/orange/mermaid-tw5/config"
      via: "$tw.wiki.getTiddlerData"
      pattern: "getTiddlerData"
    - from: "config-wiring tests (Plans 02-03)"
      to: "mockMermaidAPI.initializeCalls / lastRenderSource"
      via: "assertions on recorded calls"
      pattern: "initializeCalls"
---

<objective>
Establish the Wave 0 test infrastructure and the supporting data/config files that the
wrapper.js behavioral changes (Plans 02-03) depend on, WITHOUT touching wrapper.js itself.

This plan delivers four independent leaf artifacts:
1. Extend the test mock (`tw-bootstrap.js`) so behavioral tests can observe `initialize()`
   call counts/args and the `scriptBody` passed to `render()`. (Wave 0 requirement.)
2. Create the new JSON shadow config tiddler `$:/plugins/orange/mermaid-tw5/config`
   (the source of truth `buildSiteConfig()` will read in Plan 02; D-06 shadow-tiddler override pattern). (CONFIG-04, CONFIG-06)
3. Register the new tiddler in `plugin.info` `list` so it ships in the bundle.
4. Fix the Sankey `R&D` parse error (D-08 prerequisite that unblocks Phase 7 Sankey authoring).

Purpose: These files have ZERO overlap with wrapper.js, so they run first and in isolation;
the mock extensions are a hard prerequisite for the CONFIG-02..06 behavioral tests in Plans 02-03.
Output: Updated mock helper, new config tiddler, updated plugin.info, fixed Sankey example.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-config-wiring-foundation/06-CONTEXT.md
@.planning/phases/06-config-wiring-foundation/06-RESEARCH.md
@.planning/phases/06-config-wiring-foundation/06-PATTERNS.md
@.planning/phases/06-config-wiring-foundation/06-VALIDATION.md
</context>

<open_decisions_adopted>
- O-02 (per-type config delivery, CONFIG-06): adopt the recommended default — per-type
  config blocks (flowchart, sequence, gantt, ...) are delivered via the GLOBAL JSON config
  tiddler created here (nested JSON preserves structure), NOT via dotted tiddler fields
  (deferred to v0.7.0). The `flowchart` block in this tiddler is the canonical documented
  per-type pattern. This is the single canonical pattern; keep it consistent.
</open_decisions_adopted>

<tasks>

<task type="auto">
  <name>Task 1: Extend test mock with initialize/render call tracking</name>
  <files>tests/helpers/tw-bootstrap.js</files>
  <read_first>
    - tests/helpers/tw-bootstrap.js (the `mockMermaidAPI` object at lines 65-93; the `module.exports` block at lines 191-199 — note `mockMermaidAPI` is already exported)
    - .planning/phases/06-config-wiring-foundation/06-PATTERNS.md (section "tests/helpers/tw-bootstrap.js — Extend mock for config-wiring tests", lines 411-455 — the exact extension shape)
    - .planning/phases/06-config-wiring-foundation/06-VALIDATION.md ("Wave 0 Requirements" + "Mock Setup for New Tests")
  </read_first>
  <action>
    Extend the existing `mockMermaidAPI` object (CommonJS, already exported) with two new
    stateful fields and minimal recording logic, preserving all current behavior:
    - Add field `initializeCalls: []`.
    - Add field `lastRenderSource: null`.
    - Replace `initialize: function() {}` with `initialize: function(config) { mockMermaidAPI.initializeCalls.push(config); }`.
    - In `render: function(id, source)`, as the FIRST line of the function body, add
      `mockMermaidAPI.lastRenderSource = source;` — this must run for BOTH the INVALID_SYNTAX
      branch and the success branch, so place it before the `if (source && source.indexOf('INVALID_SYNTAX')...)` check.
    Do NOT change the thenable return shapes (the INVALID_SYNTAX rejection thenable and the
    success thenable must stay byte-for-byte as-is) — the 1 pre-existing failure and the 14
    passing tests both depend on the current thenable behavior.
    Do NOT add per-test reset logic here; tests reset `initializeCalls`/`lastRenderSource` in
    their own `beforeEach` (Plans 02-03). The mock stays module-level stateful but reset-by-test.
    `getTiddlerData` needs NO structural change — tests override `global.$tw.wiki.getTiddlerData`
    directly per-test (PATTERNS.md confirms the live object is overridable).
  </action>
  <verify>
    <automated>node --test 2>&1 | grep -E '# (pass|fail)'</automated>
  </verify>
  <acceptance_criteria>
    - `tests/helpers/tw-bootstrap.js` contains `initializeCalls: []`
    - `tests/helpers/tw-bootstrap.js` contains `lastRenderSource`
    - `tests/helpers/tw-bootstrap.js` contains `mockMermaidAPI.initializeCalls.push(config)`
    - `node --test` still reports `# pass 14` and `# fail 1` (no regression; pre-existing failure unchanged)
    - The `module.exports` block still exports `mockMermaidAPI`
  </acceptance_criteria>
  <done>Mock records initialize() args and last render source; baseline test counts unchanged (14 pass, 1 pre-existing fail).</done>
</task>

<task type="auto">
  <name>Task 2: Create JSON shadow config tiddler and register it in plugin.info</name>
  <files>mermaid-tw5/plugins/mermaid-tw5/$__plugins_mermaid-tw5_config.tid, mermaid-tw5/plugins/mermaid-tw5/plugin.info</files>
  <read_first>
    - .planning/phases/06-config-wiring-foundation/06-PATTERNS.md (section "$__plugins_mermaid-tw5_config.tid — New JSON shadow config tiddler (CREATE)", lines 178-217 — exact tiddler content; and section "plugin.info — Add config to list field", lines 221-235)
    - .planning/phases/06-config-wiring-foundation/06-RESEARCH.md (Pattern 3, lines 238-267; Pitfall 1 lines 400-408 — flowchart block must keep useMaxWidth/htmlLabels; Pitfall 5 lines 428-434 — plugin.info list field)
    - mermaid-tw5/plugins/mermaid-tw5/plugin.info (current `"list": "readme usage example license"`)
    - mermaid-tw5/plugins/mermaid-tw5/$__plugins_mermaid-tw5_readme.tid (existing .tid header convention)
  </read_first>
  <action>
    Create `$__plugins_mermaid-tw5_config.tid` with header fields `title: $:/plugins/orange/mermaid-tw5/config`,
    `type: application/json`, `tags: $:/tags/ControlPanel/Settings`, a blank line, then a JSON body
    object whose keys are: `startOnLoad` false, `securityLevel` "loose", `theme` "default",
    `look` "classic", `fontFamily` "sans-serif", and a nested `flowchart` block containing
    `useMaxWidth` true and `htmlLabels` true. The `flowchart` block MUST include both
    useMaxWidth and htmlLabels to preserve the historic plugin defaults under shallow-replace (D-04 / Pitfall 1).
    Follow the existing plain-text `.tid` header convention (title field first, blank line separates
    metadata from body, no created/modified timestamps in bundle tiddlers).
    Then edit `plugin.info`: change the `list` field value from
    `readme usage example license` to `readme usage example license config` (append the single
    token `config`, matching the new file's filename fragment) so the tiddler ships in the bundle.
    Per D-07 `startOnLoad` is present here as a documented default but Plan 02's buildSiteConfig()
    will refuse to let it be overridden — the value here is informational.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='mermaid-tw5/plugins/mermaid-tw5/\$__plugins_mermaid-tw5_config.tid';const t=fs.readFileSync(p,'utf8');const body=t.slice(t.indexOf('\n\n')+2);const j=JSON.parse(body);if(j.securityLevel!=='loose')throw new Error('securityLevel');if(j.startOnLoad!==false)throw new Error('startOnLoad');if(!j.flowchart||j.flowchart.useMaxWidth!==true||j.flowchart.htmlLabels!==true)throw new Error('flowchart');const info=JSON.parse(fs.readFileSync('mermaid-tw5/plugins/mermaid-tw5/plugin.info','utf8'));if(info.list.split(/\s+/).indexOf('config')===-1)throw new Error('plugin.info list missing config');console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - File `mermaid-tw5/plugins/mermaid-tw5/$__plugins_mermaid-tw5_config.tid` exists
    - Its header contains `type: application/json`
    - Its header contains `title: $:/plugins/orange/mermaid-tw5/config`
    - Its JSON body parses and has `securityLevel === "loose"`, `startOnLoad === false`, `flowchart.useMaxWidth === true`, `flowchart.htmlLabels === true`
    - `plugin.info` `list` field tokens include `config`
    - `node --test` still reports `# pass 14` `# fail 1`
  </acceptance_criteria>
  <done>Config tiddler exists with valid JSON defaults and is registered in plugin.info list; no test regression.</done>
</task>

<task type="auto">
  <name>Task 3: Fix Sankey R&D parse error (D-08 prerequisite)</name>
  <files>mermaid-tw5/tiddlers/Sankey Diagram.tid</files>
  <read_first>
    - mermaid-tw5/tiddlers/Sankey Diagram.tid (lines 32-50 — `R&D` appears at line 38 inside a ``` display fence and at line 48 inside the live `$$$text/vnd.tiddlywiki.mermaid` block)
    - .planning/phases/06-config-wiring-foundation/06-RESEARCH.md (Gap 2, lines 324-330 — `&` invalid in Mermaid 11.12+ Sankey node names, issue #7528; rename to `R and D`)
    - .planning/phases/06-config-wiring-foundation/06-PATTERNS.md (section "Sankey Diagram.tid — Fix R&D parse error", lines 239-260 — fix BOTH occurrences)
  </read_first>
  <action>
    Replace every occurrence of `Gross Profit,R&D,1500` with `Gross Profit,R and D,1500` in
    `Sankey Diagram.tid`. There are two occurrences: one in the ``` display fence (so the shown
    source matches the rendered output) and one in the live `$$$text/vnd.tiddlywiki.mermaid`
    block (the actual rendered diagram). Do not rely on hardcoded line numbers — replace all
    occurrences of the `R&D` node name. Change nothing else in the tiddler (no metadata edits).
    The `&` character is the only invalid token; `R and D` has no `&`, `/`, `'`, or `-`.
  </action>
  <verify>
    <automated>test "$(grep -c 'R&D' 'mermaid-tw5/tiddlers/Sankey Diagram.tid')" = "0" && grep -q 'R and D' 'mermaid-tw5/tiddlers/Sankey Diagram.tid' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `mermaid-tw5/tiddlers/Sankey Diagram.tid` does NOT contain the string `R&D`
    - `mermaid-tw5/tiddlers/Sankey Diagram.tid` contains `R and D`
    - No other lines in the tiddler changed (only the node-name token)
  </acceptance_criteria>
  <done>Sankey example has no `&` in node names; Phase 7 Sankey authoring is unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Site owner → config tiddler | The site owner edits `$:/plugins/orange/mermaid-tw5/config` JSON; this is trusted site-owner input, distinct from per-diagram author input |
| Diagram author → example content | Sankey example content is authored data rendered by Mermaid |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | config.tid `startOnLoad` value | mitigate | The tiddler lists `startOnLoad: false` as documentation; Plan 02's `buildSiteConfig()` hardcodes and refuses override (D-07). This plan does not consume the value, so no live risk here. |
| T-06-02 | Tampering | config.tid arbitrary keys | accept (mitigated downstream) | Config tiddler may contain any JSON; the CONFIG_WHITELIST filter in Plan 02 drops non-whitelisted keys before reaching `initialize()`. This plan only ships defaults; no untrusted keys at rest. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH.md "Package Legitimacy Audit": Not applicable — all deps vendored). No install tasks present. |
</threat_model>

<verification>
- `node --test` reports `# pass 14` and `# fail 1` (the 1 fail is the documented pre-existing
  `displays a friendly error message for invalid syntax`, NOT introduced here — D-09).
- Config tiddler JSON parses and carries the documented defaults.
- `plugin.info` `list` includes `config`.
- Sankey tiddler free of `R&D`.
</verification>

<success_criteria>
- Mock exposes `initializeCalls` and `lastRenderSource` for downstream behavioral tests.
- New JSON config tiddler exists, parses, ships via plugin.info.
- Sankey `R&D` parse error fixed (both occurrences).
- 14 baseline tests stay green (D-09).
</success_criteria>

<output>
Create `.planning/phases/06-config-wiring-foundation/06-01-SUMMARY.md` when done.
</output>
